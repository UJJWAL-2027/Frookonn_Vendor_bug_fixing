import React from 'react';
import { View, Text } from 'react-native';

const VendorLogin = () => {
    return (
        <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
            <Text>Vendor Login Screen (Placeholder)</Text>
        </View>
    );
};

export default VendorLogin;
